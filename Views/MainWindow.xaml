<Window xmlns="https://github.com/avaloniaui" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:vm="clr-namespace:FuzzyComic.ViewModels;assembly=FuzzyComic" Icon="resm:FuzzyComic.Assets.avalonia-logo.ico" Title="FuzzyComic" WindowState="Maximized" HasSystemDecorations="false">
  <Design.DataContext>
    <vm:MainWindowViewModel/>
  </Design.DataContext>

  <Grid RowDefinitions="Auto,Auto">
    <DockPanel Grid.Row="0" Grid.Column="0">
      <Menu DockPanel.Dock="Top">
        <MenuItem Header="_File">
          <MenuItem Header="_Open..." Command="{Binding DoOpenComicFile}" />
          <Separator/>
          <MenuItem Header="_Exit"/>
        </MenuItem>
      </Menu>
    </DockPanel>
    <Image Grid.Row="2" Grid.Column="0" Source="{Binding CurrentPage.CurrentImage}" Cursor="Hand" />
  </Grid>

</Window>